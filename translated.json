=== wrapAll ===

<p><code>.wrapAll()</code> 函数可以接受任何可以传递给 <code>$()</code> 函数的字符串或对象来指定一个 DOM 结构。这个结构可以嵌套多个层级，但应该只包含一个最内层的元素。该结构将作为一个整体包裹在匹配元素集合中的所有元素周围。</p>
    <p>考虑以下 HTML：</p>
    <pre><code>
&lt;div class="container"&gt;
  &lt;div class="inner"&gt;Hello&lt;/div&gt;
  &lt;div class="inner"&gt;Goodbye&lt;/div&gt;
&lt;/div&gt;
    </code></pre>
    <p>使用 <code>.wrapAll()</code>，我们可以在内部 <code>&lt;div&gt;</code> 元素周围插入一个 HTML 结构，如下所示：</p>
    <pre><code>
$( ".inner" ).wrapAll( "&lt;div class='new' /&gt;");
    </code></pre>
    <p>新的 <code>&lt;div&gt;</code> 元素是在运行时创建并添加到 DOM 中。结果是一个新的 <code>&lt;div&gt;</code> 包裹在所有匹配元素周围：</p>
    <pre><code>
&lt;div class="container"&gt;
  &lt;div class="new"&gt;
    &lt;div class="inner"&gt;Hello&lt;/div&gt;
    &lt;div class="inner"&gt;Goodbye&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;
    </code></pre>

=== width ===

<p><code>.css( "width" )</code> 和 <code>.width()</code> 之间的区别在于，后者返回一个无单位的像素值（例如，<code>400</code>），而前者返回一个带单位的值（例如，<code>400px</code>）。当需要在数学计算中使用元素的宽度时，推荐使用 <code>.width()</code> 方法。</p>
      <figure>
        <img src="/resources/0042_04_04.png"/>
        <figcaption>图 1 - 测量宽度的示意图</figcaption>
      </figure>
      <p>该方法还能够找到窗口和文档的宽度。</p>
      <pre><code>
// 返回浏览器视口的宽度
$( window ).width();

// 返回 HTML 文档的宽度
$( document ).width();
      </code></pre>
      <p>请注意，<code>.width()</code> 将始终返回内容宽度，无论 CSS <code>box-sizing</code> 属性的值如何。从 jQuery 1.8 开始，这可能需要检索 CSS 宽度加上 <code>box-sizing</code> 属性，然后在元素具有 <code>box-sizing: border-box</code> 时减去任何潜在的边框和内边距。为了避免这种惩罚，使用 <code>.css( "width" )</code> 而不是 <code>.width()</code>。</p>
      <div class="warning">
        <p><strong>注意：</strong> 尽管 <code>style</code> 和 <code>script</code> 标签在绝对定位并且给定 <code>display:block</code> 时会报告 <code>.width()</code> 或 <code>height()</code> 的值，但强烈不建议在这些标签上调用这些方法。除了是不好的实践外，结果也可能不可靠。</p>
      </div>

=== visible-selector ===

<p>如果元素在文档中占用空间，则被视为可见。可见元素的宽度或高度大于零。</p>
    <p>具有 <code>visibility: hidden</code> 或 <code>opacity: 0</code> 的元素被视为可见，因为它们仍然在布局中占用空间。</p>
    <p>不在文档中的元素被视为隐藏；jQuery 无法知道它们在附加到文档时是否会可见，因为这取决于适用的样式。</p>
    <p>该选择器与 <a href="/hidden-selector/"><code>:hidden</code></a> 选择器相反。因此，所有由 <code>:visible</code> 选择的元素都不会被 <code>:hidden</code> 选择，反之亦然。</p>
    <p>所有 <code>option</code> 元素都被视为隐藏，无论其 <code>selected</code> 状态如何。</p>
    <p>在隐藏元素的动画过程中，该元素在动画结束之前被视为可见。在显示元素的动画过程中，该元素在动画开始时被视为可见。</p>
    <p>在 jQuery 1.3.2 中更改了 <code>:visible</code> 的计算方式。<a href="https://blog.jquery.com/2009/02/20/jquery-1-3-2-released/">发布说明</a> 更详细地概述了这些更改。</p>
    <p>jQuery 3 稍微修改了 <code>:visible</code> 的含义（因此也修改了 <a href="/hidden-selector/"><code>:hidden</code></a>）。从这个版本开始，如果元素具有任何布局框，包括零宽度和/或高度的框，则将被视为 <code>:visible</code>。例如，<code>br</code> 元素和没有内容的内联元素将被 <code>:visible</code> 选择器选择。</p>

=== val ===

<p>该方法通常用于设置表单字段的值。</p>
      <p><code>val()</code> 允许您传递一个元素值的数组。当处理包含 <code>&lt;input type="checkbox"&gt;</code>、<code>&lt;input type="radio"&gt;</code> 和 <code>&lt;option&gt;</code> 的 jQuery 对象时，这非常有用。在这种情况下，具有与数组中元素匹配的 <code>value</code> 的 <code>input</code> 和 <code>option</code> 将被选中或选中，而具有与数组中元素不匹配的 <code>value</code> 的将被取消选中或未选中，具体取决于类型。在 <code>&lt;input type="radio"&gt;</code> 的情况下，它们是单选组的一部分，任何先前选定的元素将被取消选中。</p>
      <p>使用此方法（或使用原生 <code>value</code> 属性）设置值不会导致 <code>change</code> 事件的分发。因此，相关的事件处理程序将不会被执行。如果您想执行它们，您应该在设置值后调用 <code>.trigger( "change" )</code>。</p>
      <p><code>.val()</code> 方法允许通过传递一个函数来设置值。从 jQuery 1.4 开始，该函数传递两个参数，当前元素的索引和其当前值：</p>
      <pre><code>
$( "input[type=text].tags" ).val(function( index, value ) {
  return value.trim();
});
      </code></pre>
      <p>此示例删除了具有 "tags" 类的文本输入值的前导和尾随空格。</p>

=== undelegate ===

<p>从 jQuery 3.0 开始，<code>.undelegate()</code> 已被弃用。自 jQuery 1.7 起，它被 <a href="/off/"><code>.off()</code></a> 方法取代，因此已经不推荐使用。</p>
    <p>该 <code>.undelegate()</code> 方法是一种移除使用 <a href="/delegate/"><code>.delegate()</code></a> 绑定的事件处理程序的方法。</p>

=== trigger ===

<p>任何使用 <code>.on()</code> 或其快捷方法附加的事件处理程序在相应事件发生时会被触发。然而，可以使用 <code>.trigger()</code> 方法手动触发。调用 <code>.trigger()</code> 会按照事件自然触发时的顺序执行处理程序：</p>
    <pre><code>
$( "#foo" ).on( "click", function() {
  alert( $( this ).text() );
});
$( "#foo" ).trigger( "click" );
   </code></pre>
    <p>自 jQuery 1.3 起，<code>.trigger()</code> 触发的事件会在 DOM 树中冒泡；事件处理程序可以通过从处理程序返回 <code>false</code> 或在传入事件的事件对象上调用 <a href="/event.stopPropagation/"><code>.stopPropagation()</code></a> 方法来停止冒泡。虽然 <code>.trigger()</code> 模拟了事件激活，包含合成的事件对象，但它并不能完美复制自然发生的事件。</p>
    <p>要通过 jQuery 触发绑定的处理程序而不触发原生事件，请使用 <a href="/triggerHandler/"><code>.triggerHandler()</code></a>。</p>
    <p>当我们使用 <code>.on()</code> 方法定义自定义事件类型时，<code>.trigger()</code> 的第二个参数可能会变得有用。例如，假设我们为元素绑定了 <code>custom</code> 事件的处理程序，而不是上面所做的内置 <code>click</code> 事件：</p>
    <pre><code>
$( "#foo" ).on( "custom", function( event, param1, param2 ) {
  alert( param1 + "\n" + param2 );
});
$( "#foo").trigger( "custom", [ "Custom", "Event" ] );
    </code></pre>
    <p>事件对象始终作为第一个参数传递给事件处理程序。也可以将参数数组传递给 <code>.trigger()</code> 调用，这些参数将跟随事件对象传递给处理程序。自 jQuery 1.6.2 起，可以传递单个字符串或数字参数，而无需将其包装在数组中。</p>
    <p>注意这里传递的额外参数与 <code>eventData</code> 参数的区别，后者用于 <a href="/on/">.on()</a> 方法。两者都是向事件处理程序传递信息的机制，但 <code>extraParameters</code> 参数允许在事件触发时确定信息，而 <code>eventData</code> 参数要求在绑定处理程序时信息已经计算好。</p>
    <p>可以在类似于 pub/sub 机制的普通 JavaScript 对象上使用 <code>.trigger()</code> 方法；当事件被触发时，绑定到该对象的任何事件处理程序都会被调用。</p>
    <div class="warning"><strong>注意：</strong>对于普通对象和除 <code>window</code> 之外的 DOM 对象，如果触发的事件名称与对象上的属性名称匹配，jQuery 将尝试将该属性作为方法调用，前提是没有事件处理程序调用 <a href="/event.preventDefault/"><code>event.preventDefault()</code></a>。如果不希望这种行为，请使用 <a href="/triggerHandler/"><code>.triggerHandler()</code></a>。</div>
    <div class="warning"><strong>注意：</strong>与 <a href="/triggerHandler/"><code>.triggerHandler()</code></a> 一样，当调用 <code>.trigger()</code> 时，如果事件名称与对象上的属性名称匹配，并以 <code>on</code> 为前缀（例如，在具有非空 <code>onclick</code> 方法的 <code>window</code> 上触发 <code>click</code>），jQuery 将尝试将该属性作为方法调用。</div>
    <div class="warning"><strong>注意：</strong>当使用一个不是类数组但仍包含 <code>length</code> 属性的普通对象触发时，应该将该对象放在数组中（例如 <code>[ { length: 1 } ]</code>）。</div>

=== toggleClass ===

<p>该方法接受一个或多个类作为参数。在第一种情况下，如果匹配元素集合中的某个元素已经具有该类，则将其移除；如果某个元素没有该类，则将其添加。例如，我们可以将 <code>.toggleClass()</code> 应用于一个简单的 <code>&lt;div&gt;</code>：</p>
      <pre><code>
&lt;div class="tumble"&gt;Some text.&lt;/div&gt;
      </code></pre>
      <p>第一次应用 <code>$( "div.tumble" ).toggleClass( "bounce" )</code> 时，我们得到以下结果：</p>
      <pre><code>
&lt;div class="tumble bounce"&gt;Some text.&lt;/div&gt;
      </code></pre>
      <p>第二次应用 <code>$( "div.tumble" ).toggleClass( "bounce" )</code> 时，<code>&lt;div&gt;</code> 类返回到单一的 <code>tumble</code> 值：</p>
      <pre><code>&lt;div class="tumble"&gt;Some text.&lt;/div&gt;</code></pre>
      <p>对同一个 <code>&lt;div&gt;</code> 应用 <code>.toggleClass( "bounce spin" )</code> 会在 <code>&lt;div class="tumble bounce spin"&gt;</code> 和 <code>&lt;div class="tumble"&gt;</code> 之间交替。</p>
      <p><code>.toggleClass()</code> 的第二种版本使用第二个参数来确定是否应添加或移除该类。如果该参数的值为 <code>true</code>，则添加该类；如果为 <code>false</code>，则移除该类。实际上，以下语句：</p>
      <pre><code>
$( "#foo" ).toggleClass( className, addOrRemove );
      </code></pre>
      <p>等同于：</p>
      <pre><code>
if ( addOrRemove ) {
  $( "#foo" ).addClass( className );
} else {
  $( "#foo" ).removeClass( className );
}
      </code></pre>
      <p><strong>自 jQuery 1.4 起</strong>，如果没有参数传递给 <code>.toggleClass()</code>，则第一次调用 <code>.toggleClass()</code> 时，元素上的所有类都会被切换。此外，自 jQuery 1.4 起，要切换的类名可以通过传递一个函数来确定。</p>
      <pre><code>
$( "div.foo" ).toggleClass(function() {
  if ( $( this ).parent().is( ".bar" ) ) {
    return "happy";
  } else {
    return "sad";
  }
});
      </code></pre>
      <p>如果其父元素具有 <code>bar</code> 类，则此示例将为 <code>&lt;div class="foo"&gt;</code> 元素切换 <code>happy</code> 类；否则，将切换 <code>sad</code> 类。</p>

=== toggle ===

<div class="warning">
      <p>注意：事件处理套件中也有一个名为 <a href="/toggle-event/">.toggle()</a> 的方法。触发哪个方法取决于传递的参数集。</p>
    </div>
    <p>没有参数时，<code>.toggle()</code> 方法简单地切换元素的可见性：</p>
    <pre><code>
$( ".target" ).toggle();
    </code></pre>
    <p>匹配的元素将立即被显示或隐藏，没有动画，通过更改 CSS <code>display</code> 属性。如果元素最初是显示的，则将其隐藏；如果是隐藏的，则将其显示。<code>display</code> 属性会根据需要保存和恢复。如果元素的 <code>display</code> 值为 <code>inline</code>，然后被隐藏和显示，它将再次以 <code>inline</code> 方式显示。</p>
    <p>当提供持续时间、普通对象或 <em>单个</em> "complete" 函数时，<code>.toggle()</code> 变为动画方法。<code>.toggle()</code> 方法同时动画化匹配元素的宽度、高度和不透明度。当这些属性在隐藏动画后达到 0 时，<code>display</code> 样式属性被设置为 <code>none</code>，以确保该元素不再影响页面布局。</p>
    <p>持续时间以毫秒为单位；较高的值表示较慢的动画，而不是较快的。字符串 <code>'fast'</code> 和 <code>'slow'</code> 可以分别表示 <code>200</code> 和 <code>600</code> 毫秒的持续时间。</p>
    <p>自 jQuery 1.4.3 起，可以使用可选字符串命名一个缓动函数。缓动函数指定动画在动画的不同点的进展速度。jQuery 库中唯一的缓动实现是默认的，称为 <code>swing</code>，以及一种以恒定速度进展的实现，称为 <code>linear</code>。通过使用插件可以获得更多的缓动函数，最著名的是 <a href="https://jqueryui.com">jQuery UI 套件</a>。</p>
    <p>如果提供，回调将在动画完成后触发。这对于将不同的动画串联在一起是有用的。回调不会传递任何参数，但 <code>this</code> 被设置为正在动画的 DOM 元素。如果多个元素被动画化，重要的是要注意回调是针对每个匹配元素执行一次，而不是针对整个动画执行一次。</p>
    <p>我们可以动画化任何元素，例如一个简单的图像：</p>
    <pre><code>
&lt;div id="clickme"&gt;
  Click here
&lt;/div&gt;
&lt;img id="book" src="book.png" alt="" width="100" height="123"&gt;
    </code></pre>
    <p>当另一个元素被点击时，我们将导致调用 <code>.toggle()</code>：</p>
    <pre><code>
$( "#clickme" ).on( "click", function() {
  $( "#book" ).toggle( "slow", function() {
    // 动画完成。
  });
});
    </code></pre>
    <p>在元素最初显示的情况下，我们可以通过第一次点击慢慢隐藏它：</p>
    <figure>
      <img class="column three" src="/resources/0042_06_09.png" alt=""/>
      <img class="column three" src="/resources/0042_06_10.png" alt=""/>
      <img class="column three" src="/resources/0042_06_11.png" alt=""/>
      <img class="column three" src="/resources/0042_06_12.png" alt=""/>
      <figcaption>图 1 - 隐藏图像时 <code>toggle()</code> 效果的示例</figcaption>
    </figure>
    <p>第二次点击将再次显示该元素：</p>
    <figure>
      <img class="column three" src="/resources/0042_06_13.png" alt=""/>
      <img class="column three" src="/resources/0042_06_14.png" alt=""/>
      <img class="column three" src="/resources/0042_06_15.png" alt=""/>
      <img class="column three" src="/resources/0042_06_16.png" alt=""/>
      <figcaption>图 2 - 显示图像时 <code>toggle()</code> 效果的示例</figcaption>
    </figure>
    <p>该方法的第二种版本接受一个布尔参数。如果该参数为 <code>true</code>，则显示匹配的元素；如果为 <code>false</code>，则隐藏元素。实际上，以下语句：</p>
    <pre><code>
$( "#foo" ).toggle( display );
    </code></pre>
    <p>等同于：</p>
    <pre><code>
if ( display === true ) {
  $( "#foo" ).show();
} else if ( display === false ) {
  $( "#foo" ).hide();
}
    </code></pre>

=== toggle-event ===

<div class="warning">
      <p>注意：此方法签名在 jQuery 1.8 中被弃用，并在 jQuery 1.9 中移除。jQuery 还提供了一个名为 <a href="/toggle/">.toggle()</a> 的动画方法，用于切换元素的可见性。触发动画还是事件方法取决于传递的参数集。</p>
    </div>
    <p>该 <code>.toggle()</code> 方法为 <code>click</code> 事件绑定一个处理程序，因此触发 <code>click</code> 的规则在这里也适用。</p>
    <p>例如，考虑以下 HTML：</p>
    <pre><code>
&lt;div id="target"&gt;
  Click here
&lt;/div&gt;
    </code></pre>
    <p>然后可以将事件处理程序绑定到 <code>&lt;div&gt;</code>：</p>
    <pre><code>
$( "#target" ).toggle(function() {
  alert( "First handler for .toggle() called." );
}, function() {
  alert( "Second handler for .toggle() called." );
});
    </code></pre>
    <p>当元素被重复点击时，消息会交替出现：</p>
    <p>
      <samp>First handler for .toggle() called.</samp>
      <br/>
      <samp>Second handler for .toggle() called.</samp>
      <br/>
      <samp>First handler for .toggle() called.</samp>
      <br/>
      <samp>Second handler for .toggle() called.</samp>
      <br/>
      <samp>First handler for .toggle() called.</samp>
    </p>
    <p>如果提供了两个以上的处理程序，<code>.toggle()</code> 将在所有处理程序之间循环。例如，如果有三个处理程序，则第一个处理程序将在第一次点击、第四次点击、第七次点击等时被调用。</p>
    <p>提供 <code>.toggle()</code> 方法是为了方便。手动实现相同的行为相对简单，如果 <code>.toggle()</code> 内置的假设证明有限制，这可能是必要的。例如，如果对同一元素应用 <code>.toggle()</code> 两次，则无法保证其正常工作。由于 <code>.toggle()</code> 在内部使用 <code>click</code> 处理程序来完成其工作，因此我们必须解除绑定 <code>click</code> 以移除与 <code>.toggle()</code> 绑定的行为，以便其他 <code>click</code> 处理程序可以被捕获。该实现还在事件上调用 <code>.preventDefault()</code>，因此如果在元素上调用了 <code>.toggle()</code>，链接将不会被跟随，按钮将不会被点击。</p>

=== text ===

<p>与 <code>.html()</code> 方法不同，<code>.text()</code> 可以在 XML 和 HTML 文档中使用。<code>.text()</code> 方法的结果是一个字符串，包含所有匹配元素的组合文本。（由于不同浏览器中的 HTML 解析器存在差异，返回的文本可能在换行和其他空白字符上有所不同。）考虑以下 HTML：</p>
      <pre><code>
&lt;div class="demo-container"&gt;
  &lt;div class="demo-box"&gt;演示框&lt;/div&gt;
  &lt;ul&gt;
    &lt;li&gt;列表项 1&lt;/li&gt;
    &lt;li&gt;列表 &lt;strong&gt;项&lt;/strong&gt; 2&lt;/li&gt;
  &lt;/ul&gt;
&lt;/div&gt;
      </code></pre>
      <p>代码 <code>$( "div.demo-container" ).text()</code> 将产生以下结果：</p>
      <p>
        <code>演示框 列表项 1 列表项 2</code>
      </p>
      <p><code>.text()</code> 方法不应在表单输入或脚本上使用。要设置或获取 <code>input</code> 或 <code>textarea</code> 元素的文本值，请使用 <a href="/val/"><code>.val()</code></a> 方法。要获取脚本元素的值，请使用 <a href="/html/"><code>.html()</code></a> 方法。</p>
      <p>从 jQuery 1.4 开始，<code>.text()</code> 方法返回文本和 CDATA 节点以及元素节点的值。</p>

=== text_1 ===

<p>与 <code>.html()</code> 方法不同，<code>.text()</code> 可以在 XML 和 HTML 文档中使用。</p>
      <p>我们需要注意，此方法会根据需要转义提供的字符串，以便它在 HTML 中正确呈现。为此，它调用 DOM 方法 <code>.createTextNode()</code>，而不将字符串解释为 HTML。考虑以下 HTML：</p>
      <pre><code>
&lt;div class="demo-container"&gt;
  &lt;div class="demo-box"&gt;演示框&lt;/div&gt;
  &lt;ul&gt;
    &lt;li&gt;列表项 1&lt;/li&gt;
    &lt;li&gt;列表 &lt;strong&gt;项&lt;/strong&gt; 2&lt;/li&gt;
  &lt;/ul&gt;
&lt;/div&gt;
      </code></pre>
      <p>代码 <code>$( "div.demo-container" ).text( "&lt;p&gt;这是一个测试.&lt;/p&gt;" );</code> 将产生以下 DOM 输出：</p>
      <pre><code>
&lt;div class="demo-container"&gt;
  &amp;lt;p&amp;gt;这是一个测试.&amp;lt;/p&amp;gt;
&lt;/div&gt;
      </code></pre>
      <p>在渲染的页面上，它将显示为标签被暴露，如下所示：</p>
      <pre><code>
&lt;p&gt;这是一个测试&lt;/p&gt;
      </code></pre>
      <p><code>.text()</code> 方法不应在输入元素上使用。对于输入字段文本，请使用 <a href="/val/">.val()</a> 方法。</p>
      <p>从 jQuery 1.4 开始，<code>.text()</code> 方法允许我们通过传入一个函数来设置文本内容。</p>
      <pre><code>
$( "ul li" ).text(function( index ) {
  return "项编号 " + ( index + 1 );
});
      </code></pre>
      <p>给定一个包含三个 <code>&lt;li&gt;</code> 元素的无序列表，此示例将产生以下 DOM 输出：</p>
      <pre><code>
&lt;ul&gt;
  &lt;li&gt;项编号 1&lt;/li&gt;
  &lt;li&gt;项编号 2&lt;/li&gt;
  &lt;li&gt;项编号 3&lt;/li&gt;
&lt;/ul&gt;
      </code></pre>

=== target-selector ===

<p>如果文档的 URI 包含片段标识符或哈希，则 <code>:target</code> 选择器将匹配具有与标识符匹配的 ID 的元素。例如，给定 URI 为 https://example.com/#foo 的文档，<code>$( "p:target" )</code> 将选择 <code>&lt;p id="foo"&gt;</code> 元素。</p>
    <p>有关此用法的进一步讨论，请参见 <a href="https://www.w3.org/TR/css3-selectors/#target-pseudo">W3C CSS 规范</a>。</p>

=== stop ===

<p>当在元素上调用 <code>.stop()</code> 时，当前正在运行的动画（如果有）会立即停止。例如，如果在调用 <code>.stop()</code> 时，元素正在通过 <code>.slideUp()</code> 隐藏，则该元素仍然会显示，但高度会是之前高度的一部分。回调函数不会被调用。</p>
    <p>如果在同一元素上调用了多个动画方法，则后续动画会被放置在该元素的效果队列中。这些动画将在第一个动画完成后开始。当调用 <code>.stop()</code> 时，队列中的下一个动画会立即开始。如果 <code>clearQueue</code> 参数的值为 <code>true</code>，则队列中的其余动画将被移除并且不会运行。</p>
    <p>如果提供了 <code>jumpToEnd</code> 参数并将其值设置为 <code>true</code>，则当前动画停止，但元素会立即获得每个 CSS 属性的目标值。在我们上面的 <code>.slideUp()</code> 示例中，元素将立即隐藏。如果提供了回调函数，则会立即调用。</p>
    <p><strong>从 jQuery 1.7 开始</strong>，如果第一个参数作为字符串提供，则仅停止队列中由该字符串表示的动画。</p>
    <p><code>.stop()</code> 方法的实用性在于我们需要在 <code>mouseenter</code> 和 <code>mouseleave</code> 上对元素进行动画时：</p>
    <pre><code>
&lt;div id="hoverme"&gt;
  悬停我
  &lt;img id="hoverme" src="book.png" alt="" width="100" height="123"&gt;
&lt;/div&gt;
    </code></pre>
    <p>我们可以通过在链中添加 <code>.stop(true, true)</code> 来创建一个不错的淡出效果，而不会出现多个排队动画的常见问题：</p>
    <pre><code>
$( "#hoverme-stop-2" ).hover(function() {
  $( this ).find( "img" ).stop( true, true ).fadeOut();
}, function() {
  $( this ).find( "img" ).stop( true, true ).fadeIn();
});
    </code></pre>
    <h2>切换动画</h2>
    <p><strong>从 jQuery 1.7 开始，</strong> 使用 <code>.stop()</code> 提前停止切换动画将触发 jQuery 的内部效果跟踪。在以前的版本中，在切换动画完成之前调用 <code>.stop()</code> 方法会导致动画失去其状态（如果 jumpToEnd 为 false）。任何后续动画将从新的“中间”状态开始，有时会导致元素消失。要观察新行为，请参见下面的最终示例。</p>
    <div class="warning">
      <p>通过将属性 <code>$.fx.off</code> 设置为 <code>true</code>，可以全局停止动画。当这样做时，所有动画方法在调用时将立即将元素设置为其最终状态，而不是显示效果。</p>
    </div>

=== slice ===

<p>给定一个表示一组 DOM 元素的 jQuery 对象，<code>.slice()</code> 方法构造一个新的 jQuery 对象，包含由 <code>start</code> 和可选的 <code>end</code> 参数指定的元素子集。提供的 <code>start</code> 索引标识集合中某个元素的位置；如果省略 <code>end</code>，则结果将包含该元素之后的所有元素。</p>
    <p>考虑一个页面上有一个简单列表：</p>
    <pre><code>
&lt;ul&gt;
  &lt;li&gt;列表项 1&lt;/li&gt;
  &lt;li&gt;列表项 2&lt;/li&gt;
  &lt;li&gt;列表项 3&lt;/li&gt;
  &lt;li&gt;列表项 4&lt;/li&gt;
  &lt;li&gt;列表项 5&lt;/li&gt;
&lt;/ul&gt;
    </code></pre>
    <p>我们可以将此方法应用于列表项集合：</p>
    <pre><code>
$( "li" ).slice( 2 ).css( "background-color", "red" );
    </code></pre>
    <p>此调用的结果是项 3、4 和 5 的背景为红色。请注意，提供的索引是从零开始的，并且指的是 jQuery 对象中的元素位置，而不是 DOM 树中的位置。</p>
    <p>end 参数允许我们进一步限制所选范围。例如：</p>
    <pre><code>
$( "li" ).slice( 2, 4 ).css( "background-color", "red" );
    </code></pre>
    <p>现在仅选择项 3 和 4。索引再次是从零开始的；范围扩展到但不包括指定的索引。</p>
    <h4>负索引</h4>
    <p>jQuery <code>.slice()</code> 方法的模式与 JavaScript 数组的 .slice() 方法相似。它模仿的一个特性是可以将负数作为 <code>start</code> 或 <code>end</code> 参数传递。如果提供了负数，则表示从集合的末尾开始的位置，而不是从开头。例如：</p>
    <pre><code>
$( "li" ).slice( -2, -1 ).css( "background-color", "red" );
    </code></pre>
    <p>这次只有列表项 4 被变为红色，因为它是从末尾的两个（<code>-2</code>）和一个（<code>-1</code>）之间的范围内的唯一项。</p>

=== serializeArray ===

<p>方法 <code>.serializeArray()</code> 创建一个 JavaScript 对象数组，准备编码为 JSON 字符串。它作用于 jQuery 的 <code>form</code> 和/或表单控件集合。控件可以是几种类型：</p>
    <pre><code>
&lt;form&gt;
  &lt;div&gt;&lt;input type="text" name="a" value="1" id="a"&gt;&lt;/div&gt;
  &lt;div&gt;&lt;input type="text" name="b" value="2" id="b"&gt;&lt;/div&gt;
  &lt;div&gt;&lt;input type="hidden" name="c" value="3" id="c"&gt;&lt;/div&gt;
  &lt;div&gt;
    &lt;textarea name="d" rows="8" cols="40"&gt;4&lt;/textarea&gt;
  &lt;/div&gt;
  &lt;div&gt;&lt;select name="e"&gt;
    &lt;option value="5" selected="selected"&gt;5&lt;/option&gt;
    &lt;option value="6"&gt;6&lt;/option&gt;
    &lt;option value="7"&gt;7&lt;/option&gt;
  &lt;/select&gt;&lt;/div&gt;
  &lt;div&gt;
    &lt;input type="checkbox" name="f" value="8" id="f"&gt;
  &lt;/div&gt;
  &lt;div&gt;
    &lt;input type="submit" name="g" value="Submit" id="g"&gt;
  &lt;/div&gt;
&lt;/form&gt;
    </code></pre>
    <p>方法 <code>.serializeArray()</code> 使用标准 W3C 规则来确定应该包含哪些元素，具体来说，元素不能被禁用，并且必须包含 <code>name</code> 属性。由于表单不是通过按钮提交的，因此没有序列化提交按钮的值。文件选择元素的数据不会被序列化。没有 <code>value</code> 属性的元素用空字符串表示。</p>
    <p>此方法可以作用于选择了单个表单控件的 jQuery 对象，例如 <code>&lt;input&gt;</code>、<code>&lt;textarea&gt;</code> 和 <code>&lt;select&gt;</code>。然而，通常选择 <code>&lt;form&gt;</code> 元素本身进行序列化更为简单：</p>
    <pre><code>
$( "form" ).on( "submit", function( event ) {
  console.log( $( this ).serializeArray() );
  event.preventDefault();
} );
    </code></pre>
    <p>这将产生以下数据结构（前提是浏览器支持 <code>console.log</code>）：</p>
    <pre><code>
[
  {
    name: "a",
    value: "1"
  },
  {
    name: "b",
    value: "2"
  },
  {
    name: "c",
    value: "3"
  },
  {
    name: "d",
    value: "4"
  },
  {
    name: "e",
    value: "5"
  }
]
    </code></pre>

=== selector ===

<div class="warning">
      <p>注意：此 API 在 jQuery 3.0 中已被移除。该属性从未可靠地指示可以用于获取当前包含在 jQuery 集合中的元素集的选择器，因为后续的遍历方法可能已更改该集合。需要在其插件中使用选择器字符串的插件可以将其作为方法的参数。例如，一个 "foo" 插件可以写成 <code>$.fn.foo = function( selector, options ) { /* 插件代码在这里 */ };</code>，使用该插件的人可以写 <code>$( "div.bar" ).foo( "div.bar", {dog: "bark"} );</code>，其中 <code>"div.bar"</code> 选择器作为 <code>.foo()</code> 的第一个参数重复。</p>
    </div>

=== selected-selector ===

<p><code>:selected</code> 选择器适用于 <code>&lt;option&gt;</code> 元素。它不适用于复选框或单选输入；对于它们，请使用 <code>:checked</code>。</p>

=== scrollTop ===

<p>垂直滚动位置与在可滚动区域上方隐藏的像素数相同。如果滚动条在最顶部，或者元素不可滚动，则该数字将为 <code>0</code>。</p>

=== root-selector ===

<p>在 HTML 中，文档的根元素，因此 <code>$(":root")</code> 选择的元素始终是 <code>&lt;html&gt;</code> 元素。</p>

=== replaceWith ===

<p><code>.replaceWith()</code> 方法从 DOM 中移除内容，并在其位置插入新内容，只需一次调用。考虑以下 DOM 结构：</p>
    <pre><code>
&lt;div class="container"&gt;
  &lt;div class="inner first"&gt;Hello&lt;/div&gt;
  &lt;div class="inner second"&gt;And&lt;/div&gt;
  &lt;div class="inner third"&gt;Goodbye&lt;/div&gt;
&lt;/div&gt;
    </code></pre>
    <p>第二个内部 <code>&lt;div&gt;</code> 可以用指定的 HTML 替换：</p>
    <pre><code>
$( "div.second" ).replaceWith( "&lt;h2&gt;New heading&lt;/h2&gt;" );
    </code></pre>
    <p>这将导致以下结构：</p>
    <pre><code>
&lt;div class="container"&gt;
  &lt;div class="inner first"&gt;Hello&lt;/div&gt;
  &lt;h2&gt;New heading&lt;/h2&gt;
  &lt;div class="inner third"&gt;Goodbye&lt;/div&gt;
&lt;/div&gt;
    </code></pre>
    <p><em>所有</em> 内部 <code>&lt;div&gt;</code> 元素可以一次性被选中：</p>
    <pre><code>
$( "div.inner" ).replaceWith( "&lt;h2&gt;New heading&lt;/h2&gt;" );
    </code></pre>
    <p>这会导致所有元素被替换：</p>
    <pre><code>&lt;div class="container"&gt;
  &lt;h2&gt;New heading&lt;/h2&gt;
  &lt;h2&gt;New heading&lt;/h2&gt;
  &lt;h2&gt;New heading&lt;/h2&gt;
&lt;/div&gt;
    </code></pre>
    <p>元素也可以被选为替换：</p>
    <pre><code>
$( "div.third" ).replaceWith( $( ".first" ) );
    </code></pre>
    <p>这将导致以下 DOM 结构：</p>
    <pre><code>
&lt;div class="container"&gt;
  &lt;div class="inner second"&gt;And&lt;/div&gt;
  &lt;div class="inner first"&gt;Hello&lt;/div&gt;
&lt;/div&gt;
    </code></pre>
    <p>这个例子演示了所选元素通过从其旧位置移动来替换目标，而不是通过克隆。</p>
    <p><code>.replaceWith()</code> 方法与大多数 jQuery 方法一样，返回 jQuery 对象，以便可以在其上链接其他方法。然而，必须注意返回的是 <em>原始</em> jQuery 对象。该对象指的是已从 DOM 中移除的元素，而不是替换它的新元素。</p>

=== removeData ===

<p><code>.removeData()</code> 方法允许我们移除之前使用 <code>.data()</code> 设置的值。当使用键名调用时，<code>.removeData()</code> 删除该特定值。当不带参数调用时，<code>.removeData()</code> 移除所有值。</p>
    <p>
      请注意，<code>.removeData()</code> 只会从 jQuery 的内部 <code>.data()</code> 缓存中移除数据，元素上的任何对应 <code>data-</code> 属性将不会被移除。因此，稍后调用 <code>.data( "key" )</code>
      将重新获取 <code>data-key</code> 属性中的值。为了防止这种情况，使用 <code>.removeAttr()</code> 结合 <code>.removeData()</code> 一起移除 <code>data-</code> 属性。在 jQuery 1.4.3 之前，<code>.data()</code> 不使用 <code>data-</code> 属性，因此这不是问题。
    </p>
    <p>批量 <code>.data()</code> 获取器只会获取一次 <code>data-</code> 属性，即使在 <code>.data()</code> 调用之间调用了 <code>.removeData()</code>。</p>
    <p><strong>自 jQuery 1.7 起</strong>，当使用键数组或空格分隔的键字符串调用时，<code>.removeData()</code> 删除该数组或字符串中每个键的值。</p>

=== removeClass ===

<p>在 jQuery 版本 1.12/2.2 之前，<code>.removeClass()</code> 方法操作所选元素的 <code>className</code> <em>属性</em>，而不是 <code>class</code> <em>属性</em>。一旦属性被更改，浏览器会相应地更新属性。这意味着当 <code>class</code> 属性被更新并且最后一个类名被移除时，浏览器可能会将属性值设置为空字符串，而不是完全移除该属性。这种行为的一个影响是该方法仅适用于具有 HTML DOM 语义的文档（例如，不是纯 XML 文档）。</p>
      <p>自 jQuery 1.12/2.2 起，这种行为已更改，以改善对 XML 文档的支持，包括 SVG。从这个版本开始，使用 <code>class</code> <em>属性</em>。因此，<code>.removeClass()</code> 可以在 XML 或 SVG 文档上使用。</p>
      <p>可以一次性移除多个类，用空格分隔，从匹配元素集中，如下所示：</p>
      <pre><code>
  $( "p" ).removeClass( "myClass yourClass" )
      </code></pre>
      <p>此方法通常与 <code>.addClass()</code> 一起使用，以便将元素的类从一个切换到另一个，如下所示：</p>
      <pre><code>
  $( "p" ).removeClass( "myClass noClass" ).addClass( "yourClass" );
      </code></pre>
      <p>在这里，<code>myClass</code> 和 <code>noClass</code> 类从所有段落中移除，而 <code>yourClass</code> 被添加。</p>
      <p>要用另一个类替换所有现有类，我们可以使用 <code>.attr( "class", "newClass" )</code>。</p>
      <p>自 jQuery 1.4 起，<code>.removeClass()</code> 方法允许我们通过传递一个函数来指示要移除的类。</p>
      <pre><code>
  $( "li" ).last().removeClass(function() {
    return $( this ).prev().attr( "class" );
  });
      </code></pre>
      <p>这个例子从倒数第二个 <code>&lt;li&gt;</code> 中移除最后一个 <code>&lt;li&gt;</code> 的类名。</p>

=== removeAttr ===

<p><code>.removeAttr()</code> 方法使用 JavaScript <code>removeAttribute()</code> 函数，但它的优点是可以直接在 jQuery 对象上调用，并且考虑了不同浏览器之间的属性命名。</p>
    <p><strong>注意：</strong> 使用 <code>.removeAttr()</code> 移除内联 <code>onclick</code> 事件处理程序在 Internet Explorer 8、9 和 11 中无法达到预期效果。为了避免潜在问题，请使用 <code>.prop()</code>：</p>
    <pre><code>
$element.prop( "onclick", null );
console.log( "onclick property: ", $element[ 0 ].onclick );
    </code></pre>
    <note id="svg-support" type="additional"/>

=== queue ===

<p>每个元素可以通过 jQuery 附加一个或多个函数队列。在大多数应用中，仅使用一个队列（称为 <code>fx</code>）。队列允许在元素上异步调用一系列操作，而不会停止程序执行。典型的例子是对一个元素调用多个动画方法。例如：</p>
      <pre><code>
$( "#foo" ).slideUp().fadeIn();
      </code></pre>
      <p>当执行此语句时，元素立即开始滑动动画，但淡入过渡被放置在 <code>fx</code> 队列中，仅在滑动过渡完成后调用。</p>
      <p><code>.queue()</code> 方法允许我们直接操作这个函数队列。使用回调调用 <code>.queue()</code> 特别有用；它允许我们将一个新函数放在队列的末尾。回调函数在 jQuery 集合中的每个元素上执行一次。</p>
      <p>此功能类似于在动画方法中提供回调函数，但不要求在执行动画时给出回调。</p>
      <pre><code>
$( "#foo" ).slideUp();
$( "#foo" ).queue(function() {
  alert( "动画完成。" );
  $( this ).dequeue();
});
      </code></pre>
      <p>这相当于：</p>
      <pre><code>
$( "#foo" ).slideUp(function() {
  alert( "动画完成。" );
});
      </code></pre>
      <p>请注意，当使用 <code>.queue()</code> 添加函数时，我们应该确保最终调用 <code>.dequeue()</code>，以便下一个函数在队列中执行。</p>
      <p><strong>自 jQuery 1.4 起</strong>，被调用的函数会将另一个函数作为第一个参数传递。当调用时，这会自动出队下一个项目并保持队列移动。我们使用如下：</p>
      <pre><code>
$( "#test" ).queue(function( next ) {
    // 做一些事情...
    next();
});
    </code></pre>

=== prop ===

<p><code>.prop()</code> 方法仅获取匹配集中的 <em>第一个</em> 元素的属性值。如果属性未设置或匹配集没有元素，则返回 <code>undefined</code>。要获取每个元素的值，请使用循环结构，例如 jQuery 的 <code>.each()</code> 或 <code>.map()</code> 方法。</p>
      <div class="warning">
        <p><strong>注意：</strong>尝试更改通过 HTML 创建的 <code>input</code> 元素的 <code>type</code> 属性（或属性）将导致 Internet Explorer 6、7 或 8 抛出错误。</p>
      </div>
      <h4>属性与属性</h4>
      <p><em>属性</em> 和 <em>属性</em> 之间的区别在特定情况下可能很重要。<strong>在 jQuery 1.6 之前</strong>，<code><a href="/attr/">.attr()</a></code> 方法在检索某些属性时有时会考虑属性值，这可能导致不一致的行为。<strong>自 jQuery 1.6 起</strong>，<code>.prop()</code> 方法提供了一种显式检索属性值的方法，而 <code>.attr()</code> 则检索属性。</p>
      <p>例如，<code>selectedIndex</code>、<code>tagName</code>、<code>nodeName</code>、<code>nodeType</code>、<code>ownerDocument</code>、<code>defaultChecked</code> 和 <code>defaultSelected</code> 应使用 <code>.prop()</code> 方法进行检索和设置。在 jQuery 1.6 之前，这些属性可以通过 <code>.attr()</code> 方法检索，但这不在 <code>attr</code> 的范围内。这些没有对应的属性，仅是属性。</p>
      <p>关于布尔属性，考虑一个由 HTML 标记 <code>&lt;input type="checkbox" checked="checked" /&gt;</code> 定义的 DOM 元素，并假设它在一个名为 <code>elem</code> 的 JavaScript 变量中：</p>
      <table>
        <tr>
          <th>
            <code>elem.checked</code>
          </th>
          <td><code>true</code> (布尔) 将随复选框状态变化</td>
        </tr>
        <tr>
          <th>
            <code>$( elem ).prop( "checked" )</code>
          </th>
          <td><code>true</code> (布尔) 将随复选框状态变化</td>
        </tr>
        <tr>
          <th>
            <code>elem.getAttribute( "checked" )</code>
          </th>
          <td><code>"checked"</code> (字符串) 复选框的初始状态；不变</td>
        </tr>
        <tr>
          <th>
            <code>$( elem ).attr( "checked" )</code>
            <em>(1.6+)</em>
          </th>
          <td><code>"checked"</code> (字符串) 复选框的初始状态；不变</td>
        </tr>
        <tr>
          <th>
            <code>$( elem ).attr( "checked" )</code>
            <em>(pre-1.6)</em>
          </th>
          <td><code>true</code> (布尔) 随复选框状态变化</td>
        </tr>
      </table>
      <br/>
      <p>根据 <a href="https://www.w3.org/TR/html401/interact/forms.html#h-17.4">W3C 表单规范</a>，<code>checked</code> 属性是 <em><a href="https://www.w3.org/TR/html4/intro/sgmltut.html#h-3.3.4.2">布尔属性</a></em>，这意味着如果属性存在，则对应的属性为 <strong>true</strong>——即使，例如，属性没有值或设置为空字符串值或甚至 "false"。所有布尔属性都是如此。</p>
      <p>然而，关于 <code>checked</code> 属性最重要的概念是，它与 <code>checked</code> 属性不对应。该属性实际上对应于 <code>defaultChecked</code> 属性，仅应用于设置复选框的 <em>初始</em> 值。<code>checked</code> 属性值不会随复选框的状态变化，而 <code>checked</code> 属性会。因此，确定复选框是否被选中的跨浏览器兼容方法是使用属性：</p>
      <ul>
        <li>
          <code>if ( elem.checked )</code>
        </li>
        <li>
          <code>if ( $( elem ).prop( "checked" ) )</code>
        </li>
        <li>
          <code>if ( $( elem ).is( ":checked" ) )</code>
        </li>
      </ul>
      <p其他动态属性（如 <code>selected</code> 和 <code>value</code>）也是如此。</p>

=== prevUntil ===

<p>给定一个表示一组 DOM 元素的选择器表达式，<code>.prevUntil()</code> 方法在 DOM 树中搜索这些元素的前驱，直到达到由方法参数匹配的元素为止。返回的新 jQuery 对象包含所有前面的兄弟，直到但不包括由 <code>.prevUntil()</code> 选择器匹配的元素；这些元素按从最近的兄弟到最远的顺序返回。</p>
    <p>如果未匹配选择器或未提供选择器，则将选择所有前面的兄弟；在这些情况下，它选择与 <code><a href="/prevAll/">.prevAll()</a></code> 方法在未提供过滤选择器时相同的元素。</p>
    <p><strong>自 jQuery 1.6 起</strong>，可以使用 DOM 节点或 jQuery 对象作为第一个 <strong>.prevUntil()</strong> 参数，而不是选择器。</p>
    <p>该方法可选择接受选择器表达式作为其第二个参数。如果提供了此参数，将通过测试它们是否匹配来过滤元素。</p>

=== prev ===

<p>给定一个表示一组 DOM 元素的 jQuery 对象，<code>.prev()</code> 方法在 DOM 树中搜索每个这些元素的前驱，并从匹配的元素构造一个新的 jQuery 对象。</p>
    <p>该方法可选择接受可以传递给 <code>$()</code> 函数的相同类型的选择器表达式。如果提供了选择器，则前一个元素将通过测试是否匹配选择器进行过滤。</p>
    <p>考虑一个页面上有一个简单列表：</p>
    <pre><code>
&lt;ul&gt;
  &lt;li&gt;列表项 1&lt;/li&gt;
  &lt;li&gt;列表项 2&lt;/li&gt;
  &lt;li class="third-item"&gt;列表项 3&lt;/li&gt;
  &lt;li&gt;列表项 4&lt;/li&gt;
  &lt;li&gt;列表项 5&lt;/li&gt;
&lt;/ul&gt;
    </code></pre>
    <p>要选择紧接着列表项三的元素：</p>
    <pre><code>
$( "li.third-item" ).prev().css( "background-color", "red" );
    </code></pre>
    <p>此调用的结果是列表项 2 背后有一个红色背景。由于未提供选择器表达式，因此此前一个元素毫无疑问地作为对象的一部分被包含。如果提供了选择器，则在包含之前将测试该元素是否匹配。</p>
    <p>如果不存在前一个兄弟，或者前一个兄弟元素不匹配提供的选择器，则返回一个空的 jQuery 对象。</p>
    <p>要选择 <em>所有</em> 前置兄弟元素，而不仅仅是前一个 <em>相邻</em> 兄弟，请使用 <a href="/prevAll/">.prevAll()</a> 方法。</p>

=== prepend ===

<p><code>.prepend()</code> 方法将指定的内容作为每个 jQuery 集合中元素的第一个子元素插入（要将其作为 <em>最后</em> 子元素插入，请使用 <a href="/append/"><code>.append()</code></a>）。 </p>
    <p><code>.prepend()</code> 和 <code><a href="/prependTo/">.prependTo()</a></code> 方法执行相同的任务。主要区别在于语法——具体来说，在内容和目标的放置上。使用 <code>.prepend()</code> 时，方法前面的选择器表达式是内容插入的容器。另一方面，使用 <code>.prependTo()</code> 时，内容在方法之前，无论是作为选择器表达式还是动态创建的标记，并插入到目标容器中。</p>
    <p>考虑以下 HTML：</p>
    <pre><code>
&lt;h2&gt;问候&lt;/h2&gt;
&lt;div class="container"&gt;
  &lt;div class="inner"&gt;你好&lt;/div&gt;
  &lt;div class="inner"&gt;再见&lt;/div&gt;
&lt;/div&gt;
    </code></pre>
    <p>您可以创建内容并将其插入多个元素：</p>
    <pre><code>
$( ".inner" ).prepend( "&lt;p&gt;测试&lt;/p&gt;" );
    </code></pre>
    <p>每个 <code>&lt;div class="inner"&gt;</code> 元素都获得这个新内容：</p>
    <pre><code>
&lt;h2&gt;问候&lt;/h2&gt;
&lt;div class="container"&gt;
  &lt;div class="inner"&gt;
    &lt;p&gt;测试&lt;/p&gt;
    你好
  &lt;/div&gt;
  &lt;div class="inner"&gt;
    &lt;p&gt;测试&lt;/p&gt;
    再见
  &lt;/div&gt;
&lt;/div&gt;
</code></pre>
    <p>您还可以选择页面上的一个元素并将其插入到另一个元素中：</p>
    <pre><code>
$( ".container" ).prepend( $( "h2" ) );
    </code></pre>
    <p>如果以这种方式选择的 <em>单个元素</em> 被插入到 DOM 中的单个位置，它将被移动到目标中（<em>而不是克隆</em>）：</p>
    <pre><code>
&lt;div class="container"&gt;
    &lt;h2&gt;问候&lt;/h2&gt;
    &lt;div class="inner"&gt;你好&lt;/div&gt;
    &lt;div class="inner"&gt;再见&lt;/div&gt;
&lt;/div&gt;
</code></pre>
    <p><strong>重要</strong>：但是，如果有多个目标元素，则会为每个目标创建插入元素的克隆副本，除了最后一个。</p>
    <h4 id="additional-arguments">附加参数</h4>
    <p>与其他内容添加方法（如 <code><a href="/append/">.append()</a></code> 和 <code><a href="/before/">.before()</a></code>）类似，<code>.prepend()</code> 也支持传入多个参数作为输入。支持的输入包括 DOM 元素、jQuery 对象、HTML 字符串和 DOM 元素数组。</p>
    <p>例如，以下将插入两个新的 <code>&lt;div&gt;</code> 和一个现有的 <code>&lt;div&gt;</code> 作为 body 的前三个子节点：</p>
    <pre><code>
var $newdiv1 = $( "&lt;div id='object1'&gt;&lt;/div&gt;"),
  newdiv2 = document.createElement( "div" ),
  existingdiv1 = document.getElementById( "foo" );

$( "body" ).prepend( $newdiv1, [ newdiv2, existingdiv1 ] );
    </code></pre>
    <p>由于 <code>.prepend()</code> 可以接受任意数量的附加参数，因此可以通过将三个 <code>&lt;div&gt;</code> 作为三个单独的参数传入来实现相同的结果，如：<code>$( "body" ).prepend( $newdiv1, newdiv2, existingdiv1 )</code>。参数的类型和数量将主要取决于您在代码中收集元素的方式。</p>

=== parent ===

<p>给定一个表示一组 DOM 元素的 jQuery 对象，<code>parent()</code> 方法遍历这些元素在 DOM 树中的直接父元素，并从匹配的元素构造一个新的 jQuery 对象。</p>
    <p>此方法类似于 <a href="/parents/"><code>.parents()</code></a>，但 <code>.parent()</code> 仅向上遍历一层 DOM 树。此外，<code>$( "html" ).parent()</code> 方法返回一个包含 <code>document</code> 的集合，而 <code>$( "html" ).parents()</code> 返回一个空集合。</p>
    <p>该方法可选地接受一个与我们可以传递给 <code>$()</code> 函数相同类型的选择器表达式。如果提供了选择器，元素将通过测试是否匹配该选择器进行过滤。</p>
    <p>考虑一个包含基本嵌套列表的页面：</p>
    <pre><code>
&lt;ul class="level-1"&gt;
  &lt;li class="item-i"&gt;I&lt;/li&gt;
  &lt;li class="item-ii"&gt;II
    &lt;ul class="level-2"&gt;
      &lt;li class="item-a"&gt;A&lt;/li&gt;
      &lt;li class="item-b"&gt;B
        &lt;ul class="level-3"&gt;
          &lt;li class="item-1"&gt;1&lt;/li&gt;
          &lt;li class="item-2"&gt;2&lt;/li&gt;
          &lt;li class="item-3"&gt;3&lt;/li&gt;
        &lt;/ul&gt;
      &lt;/li&gt;
      &lt;li class="item-c"&gt;C&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li class="item-iii"&gt;III&lt;/li&gt;
&lt;/ul&gt;
    </code></pre>
    <p>如果我们从项目 A 开始，我们可以找到它的父元素：</p>
    <pre><code>
$( "li.item-a" ).parent().css( "background-color", "red" );
    </code></pre>
    <p>此调用的结果是将二级列表的背景颜色更改为红色。由于我们没有提供选择器表达式，父元素毫无疑问地被包含在对象中。如果我们提供了选择器，则会在包含之前测试该元素是否匹配。</p>

=== parent-selector ===

<p>这是 <code>:empty</code> 的反义词。</p>
    <p>关于使用 <code>:parent</code>（和 <code>:empty</code>）需要注意的一件重要事情是，子节点包括文本节点。</p>
    <p>W3C 建议 <code>&lt;p&gt;</code> 元素至少有一个子节点，即使该子节点仅仅是文本（见 <a href="https://www.w3.org/TR/html401/struct/text.html#edef-P">https://www.w3.org/TR/html401/struct/text.html#edef-P</a>）。另一方面，一些其他元素根据定义是空的（即没有子节点）：例如 <code>&lt;input&gt;</code>、<code>&lt;img&gt;</code>、<code>&lt;br&gt;</code> 和 <code>&lt;hr&gt;</code>。</p>
    <p>要获取现有 jQuery 集合的父元素或祖先，请参见 <code><a href="/parent/">.parent()</a></code> 和 <code><a href="/parents/">.parents()</a></code> 方法。</p>

=== only-of-type-selector ===

<p>如果父元素有其他同名子元素，则不匹配任何元素。</p>

=== on ===

<p><code>.on()</code> 方法将事件处理程序附加到 jQuery 对象中当前选定的元素集合。自 jQuery 1.7 起，<code>.on()</code> 方法提供了附加事件处理程序所需的所有功能。有关从旧的 jQuery 事件方法转换的帮助，请参见 <a href="/bind/"><code>.bind()</code></a>、<a href="/delegate/"><code>.delegate()</code></a> 和 <a href="/live/"><code>.live()</code></a>。要删除使用 <code>.on()</code> 绑定的事件，请参见 <a href="/off/"><code>.off()</code></a>。要附加一个只运行一次然后自我移除的事件，请参见 <a href="/one/"><code>.one()</code></a></p>
    <h2 id="event-names">事件名称和命名空间</h2>
    <p>可以将任何事件名称用于 <code>events</code> 参数。jQuery 将通过浏览器的标准 JavaScript 事件类型传递，调用 <code>handler</code> 函数，当浏览器因用户操作（如 <code>click</code>）生成事件时。此外，<a href="/trigger/"><code>.trigger()</code></a> 方法可以触发标准浏览器事件名称和自定义事件名称以调用附加的处理程序。事件名称应仅包含字母数字、下划线和冒号字符。</p>
    <p>事件名称可以通过 <em>事件命名空间</em> 进行限定，以简化事件的移除或触发。例如，<code>"click.myPlugin.simple"</code> 为此特定点击事件定义了 myPlugin 和 simple 命名空间。通过该字符串附加的点击事件处理程序可以通过 <code>.off("click.myPlugin")</code> 或 <code>.off("click.simple")</code> 移除，而不会干扰附加到元素的其他点击处理程序。命名空间类似于 CSS 类，因为它们不是层次结构的；只需匹配一个名称即可。命名空间应仅包含字母和数字。</p>
    <p>在 <code>.on()</code> 的第二种形式中，<code>events</code> 参数是一个普通对象。键是与 <code>events</code> 参数相同形式的字符串，包含以空格分隔的事件类型名称和可选命名空间。每个键的值是一个函数（或 <code>false</code> 值），用作 <code>handler</code>，而不是方法的最后一个参数。在其他方面，这两种形式在行为上是相同的，如下所述。</p>
    <h2 id="direct-and-delegated-events">直接和委托事件处理程序</h2>
    <p>大多数浏览器事件 <em>冒泡</em>，或 <em>传播</em>，从文档中发生的最深、最内层元素（<strong>事件目标</strong>）一直到 body 和 <code>document</code> 元素。在 Internet Explorer 8 及更早版本中，某些事件（如 <code>change</code> 和 <code>submit</code>）不会原生冒泡，但 jQuery 对这些事件进行了修补，使其能够冒泡并创建一致的跨浏览器行为。</p>
    <p>如果省略 <code>selector</code> 或为 null，则事件处理程序称为 <em>直接</em> 或 <em>直接绑定</em>。每当在选定元素上发生事件时，无论是直接在元素上发生还是从子元素（内部元素）冒泡，处理程序都会被调用。</p>
    <p>当提供 <code>selector</code> 时，事件处理程序称为 <em>委托</em>。当事件直接发生在绑定元素上时，不会调用处理程序，而仅对匹配选择器的子元素（内部元素）调用。jQuery 将事件从事件目标冒泡到附加处理程序的元素（即从内层到外层元素），并对沿该路径匹配选择器的任何元素运行处理程序。</p>
    <p><strong>事件处理程序仅绑定到当前选定的元素；它们必须在您的代码调用 <code>.on()</code> 时存在</strong>。为了确保元素存在并可以被选中，请将脚本放在 HTML 标记中的元素之后，或在文档就绪处理程序内执行事件绑定。或者，使用委托事件处理程序来附加事件处理程序。</p>
    <p><strong>委托事件处理程序</strong> 的优势在于它们可以处理稍后添加到文档中的 <em>子元素</em> 的事件。通过选择一个在附加委托事件处理程序时保证存在的元素，您可以使用委托事件处理程序来避免频繁附加和移除事件处理程序的需要。这个元素可以是模型-视图-控制器设计中视图的容器元素，或者如果事件处理程序想要监控文档中的所有冒泡事件，则可以是 <code>document</code>。<code>document</code> 元素在加载任何其他 HTML 之前可在文档的 <code>head</code> 中使用，因此在不等待文档准备就绪的情况下安全地在此处附加事件。</p>
    <p>除了能够处理尚未创建的子元素的事件外，委托事件处理程序的另一个优势是当必须监控许多元素时，它们的开销可能会更低。在一个包含 1,000 行的 <code>tbody</code> 的数据表中，此示例将处理程序附加到 1,000 个元素：</p>
    <pre><code>
$( "#dataTable tbody tr" ).on( "click", function() {
  console.log( $( this ).text() );
});
    </code></pre>
    <p>事件委托方法仅将事件处理程序附加到一个元素，即 tbody，事件只需向上冒泡一层（从被点击的 <code>tr</code> 到 <code>tbody</code>）：</p>
    <pre><code>
$( "#dataTable tbody" ).on( "click", "tr", function() {
  console.log( $( this ).text() );
});
    </code></pre>
    <p><strong>注意：</strong> 委托事件处理程序不适用于 SVG。</p>
    <h2 id="event-handler">事件处理程序及其环境</h2>
    <p><code>handler</code> 参数是一个函数（或值 <code>false</code>，见下文），除非您为 <code>events</code> 参数传递一个对象，否则这是必需的。您可以在 <code>.on()</code> 调用时提供一个匿名处理程序函数，如上面的示例所示，或声明一个命名函数并传递其名称：</p>
    <pre><code>
function notify() {
  alert( "clicked" );
}
$( "button" ).on( "click", notify );
    </code></pre>
    <p>当浏览器触发事件或其他 JavaScript 调用 jQuery 的 <code>.trigger()</code> 方法时，jQuery 将向处理程序传递一个 <a href="/category/events/event-object/"><code>Event</code></a> 对象，您可以使用该对象分析和更改事件的状态。该对象是浏览器提供的数据的 <em>标准化子集</em>；浏览器未修改的原生事件对象可在 <code>event.originalEvent</code> 中使用。例如，<a href="/event.type/"><code>event.type</code></a> 包含事件名称（例如，"resize"），而 <a href="/event.target/"><code>event.target</code></a> 指示事件发生的最深（内层）元素。</p>
    <p>默认情况下，大多数事件从原始事件目标冒泡到 <code>document</code> 元素。在沿途的每个元素上，jQuery 调用任何已附加的匹配事件处理程序。处理程序可以通过调用 <code>event.stopPropagation()</code> 来阻止事件进一步冒泡（从而防止在这些元素上运行处理程序）。然而，当前元素上附加的任何其他处理程序 <em>仍然会</em> 运行。要阻止这一点，请调用 <code>event.stopImmediatePropagation()</code>。（绑定到元素的事件处理程序按绑定的顺序调用。）</p>
    <p>同样，处理程序可以调用 <code>event.preventDefault()</code> 来取消浏览器可能对该事件的任何默认操作；例如，<code>click</code> 事件的默认操作是跟随链接。并非所有浏览器事件都有默认操作，并且并非所有默认操作都可以被取消。有关详细信息，请参见 <a href="https://www.w3.org/TR/DOM-Level-3-Events/#event-types-list">W3C 事件规范</a>。</p>
    <p>从事件处理程序返回 <code>false</code> 将自动调用 <code>event.stopPropagation()</code> 和 <code>event.preventDefault()</code>。也可以将 <code>false</code> 值传递给 <code>handler</code>，作为 <code>function(){ return false; }</code> 的简写。因此，<code>$( "a.disabled" ).on( "click", false );</code> 将事件处理程序附加到所有类为 "disabled" 的链接，当它们被点击时，防止它们被跟随，并且还停止事件冒泡。</p>
    <p>当 jQuery 调用处理程序时，<code>this</code> 关键字是对事件传递的元素的引用；对于直接绑定的事件，这是附加事件的元素，对于委托事件，这是匹配 <code>selector</code> 的元素。（请注意，如果事件从子元素冒泡，则 <code>this</code> 可能不等于 <code>event.target</code>。）要从元素创建 jQuery 对象，以便可以与 jQuery 方法一起使用，请使用 <code>$( this )</code>。</p>
    <h2 id="passing-data">将数据传递给处理程序</h2>
    <p>如果为 <code>.on()</code> 提供了 <code>data</code> 参数，并且不为 <code>null</code> 或 <code>undefined</code>，则每次触发事件时，它将通过 <a href="/event.data/"><code>event.data</code></a> 属性传递给处理程序。<code>data</code> 参数可以是任何类型，但如果使用字符串，则必须提供 <code>selector</code> 或显式传递为 <code>null</code>，以便数据不会被误认为是选择器。最佳实践是使用普通对象，以便可以将多个值作为属性传递。</p>
    <p>自 jQuery 1.4 起，同一事件处理程序可以多次绑定到元素。这在使用 <code>event.data</code> 特性时特别有用，或者当其他唯一数据位于事件处理程序函数周围的闭包中时。例如：</p>
    <pre><code>
function greet( event ) {
  alert( "Hello " + event.data.name );
}
$( "button" ).on( "click", {
  name: "Karl"
}, greet );
$( "button" ).on( "click", {
  name: "Addy"
}, greet );
    </code></pre>
    <p>上述代码在按钮被点击时将生成两个不同的警报。</p>
    <p>作为提供给 <code>.on()</code> 方法的 <code>data</code> 参数的替代或补充，您还可以使用第二个参数将数据传递给事件处理程序，使用 <a href="/trigger/"><code>.trigger()</code></a> 或 <a href="/triggerHandler/"><code>.triggerHandler()</code></a>。以这种方式提供的数据在 <code>Event</code> 对象之后作为进一步的参数传递给事件处理程序。如果将数组传递给 <code>.trigger()</code> 或 <code>.triggerHandler()</code> 的第二个参数，则数组中的每个元素将作为单独的参数呈现给事件处理程序。</p>
    <h2 id="event-performance">事件性能</h2>
    <p>在大多数情况下，像 <code>click</code> 这样的事件发生频率不高，性能不是一个重要问题。然而，像 <code>mousemove</code> 或 <code>scroll</code> 这样的高频事件每秒可能触发数十次，在这些情况下，明智地使用事件变得更加重要。通过减少处理程序本身所做的工作、缓存处理程序所需的信息而不是重新计算，或通过使用 <code>setTimeout</code> 限制实际页面更新的数量，可以提高性能。</p>
    <p>在文档树的顶部附近附加许多委托事件处理程序可能会降低性能。每次事件发生时，jQuery 必须将所有附加事件的选择器与从事件目标到文档顶部的每个元素进行比较。为了获得最佳性能，请在尽可能接近目标元素的文档位置附加委托事件。避免在大型文档中对 <code>document</code> 或 <code>document.body</code> 的过度使用。</p>
    <p>当用于过滤委托事件时，jQuery 可以非常快速地处理形式为 <code>tag#id.class</code> 的简单选择器。因此，<code>"#myForm"</code>、<code>"a.external"</code> 和 <code>"button"</code> 都是快速选择器。使用更复杂选择器（特别是层次选择器）的委托事件可能会慢几倍——尽管对于大多数应用程序来说，它们仍然足够快。层次选择器通常可以通过将处理程序附加到文档中的更合适位置来避免。例如，不要使用 <code>$( "body" ).on( "click", "#commentForm .addNew", addComment )</code>，而是使用 <code>$( "#commentForm" ).on( "click", ".addNew", addComment )</code>。</p>
    <h2 id="additional-notes">附加说明</h2>
    <p>某些事件有专门的页面，描述其使用的具体细节。有关这些事件的完整列表，请参见 <a href="/category/events/">事件类别</a>。</p>
    <p><em>在 jQuery 1.8 中弃用，在 1.9 中移除：</em>名称 <code>"hover"</code> 用作字符串 <code>"mouseenter mouseleave"</code> 的简写。它为这两个事件附加了一个 <em>单一事件处理程序</em>，处理程序必须检查 <code>event.type</code> 以确定事件是 <code>mouseenter</code> 还是 <code>mouseleave</code>。不要将 "hover" 伪事件名称与 <a href="/hover/"><code>.hover()</code></a> 方法混淆，该方法接受 <em>一个或两个</em> 函数。</p>
    <p>jQuery 的事件系统要求 DOM 元素允许通过元素上的属性附加数据，以便可以跟踪和传递事件。<code>object</code>、<code>embed</code> 和 <code>applet</code> 元素无法附加数据，因此无法将 jQuery 事件绑定到它们。</p>
    <p><code>focus</code> 和 <code>blur</code> 事件由 W3C 指定为不冒泡，但 jQuery 定义了跨浏览器的 <code>focusin</code> 和 <code>focusout</code> 事件，这些事件会冒泡。当使用 <code>focus</code> 和 <code>blur</code> 附加委托事件处理程序时，jQuery 将名称映射并分别传递为 <code>focusin</code> 和 <code>focusout</code>。为了保持一致性和清晰性，请使用冒泡事件类型名称。</p>
    <p>在所有浏览器中，<code>load</code>、<code>scroll</code> 和 <code>error</code> 事件（例如，在 <code>&lt;img&gt;</code> 元素上）不会冒泡。在 Internet Explorer 8 及更早版本中，<code>paste</code> 和 <code>reset</code> 事件不会冒泡。这些事件不支持用于委托，但可以在事件处理程序直接附加到生成事件的元素时使用。</p>
    <p><code>window</code> 对象上的 <code>error</code> 事件使用非标准参数和返回值约定，因此 jQuery 不支持它。相反，直接将处理程序函数分配给 <code>window.onerror</code> 属性。</p>
    <p>元素的处理程序列表在事件首次传递时设置。在当前元素上添加或移除事件处理程序不会在下次处理事件时生效。要阻止在事件处理程序内的元素上执行任何进一步的事件处理程序，请调用 <code>event.stopImmediatePropagation()</code>。这种行为违反了 <a href="https://www.w3.org/TR/DOM-Level-2-Events/events.html#Events-EventTarget-removeEventListener">W3C 事件规范</a>。要更好地理解这种情况，请考虑以下代码：</p>
    <pre><code>var $test = $( "#test" );

function handler1() {
  console.log( "handler1" );
  $test.off( "click", handler2 );
}

function handler2() {
  console.log( "handler2" );
}

$test.on( "click", handler1 );
$test.on( "click", handler2 );</code></pre>
    <p>在上述代码中，即使使用 <a href="/off/"><code>.off()</code></a> 移除，<code>handler2</code> 在第一次触发时仍会执行。然而，在后续触发 <code>click</code> 事件时，该处理程序将不会被执行。</p>

=== offsetParent ===

<p>给定一个表示一组 DOM 元素的 jQuery 对象，<code>.offsetParent()</code> 方法允许我们在 DOM 树中搜索这些元素的祖先，并构造一个新的 jQuery 对象，包装最近的定位祖先。如果一个元素的 CSS position 属性为 <code>relative</code>、<code>absolute</code> 或 <code>fixed</code>，则称该元素为定位元素。此信息对于计算偏移以执行动画和在页面上放置对象非常有用。</p>
    <p>考虑一个包含基本嵌套列表的页面，其中有一个定位元素：</p>
    <pre><code>
&lt;ul class="level-1"&gt;
  &lt;li class="item-i"&gt;I&lt;/li&gt;
  &lt;li class="item-ii" style="position: relative;"&gt;II
    &lt;ul class="level-2"&gt;
      &lt;li class="item-a"&gt;A&lt;/li&gt;
      &lt;li class="item-b"&gt;B
        &lt;ul class="level-3"&gt;
          &lt;li class="item-1"&gt;1&lt;/li&gt;
          &lt;li class="item-2"&gt;2&lt;/li&gt;
          &lt;li class="item-3"&gt;3&lt;/li&gt;
        &lt;/ul&gt;
      &lt;/li&gt;
      &lt;li class="item-c"&gt;C&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li class="item-iii"&gt;III&lt;/li&gt;
&lt;/ul&gt;
    </code></pre>
    <p>如果我们从项目 A 开始，我们可以找到它的定位祖先：</p>
    <pre><code>
$( "li.item-a" ).offsetParent().css( "background-color", "red" );
    </code></pre>
    <p>这将改变列表项 II 的颜色，该项是定位的。</p>

=== odd ===

<p>给定一个表示一组 DOM 元素的 jQuery 对象，<code>.odd()</code> 方法从该集合中的奇数元素构造一个新的 jQuery 对象。计数从零开始！</p>
    <p>考虑一个页面上有一个简单列表：</p>
    <pre><code>
&lt;ul&gt;
  &lt;li&gt;列表项 1&lt;/li&gt;
  &lt;li&gt;列表项 2&lt;/li&gt;
  &lt;li&gt;列表项 3&lt;/li&gt;
  &lt;li&gt;列表项 4&lt;/li&gt;
  &lt;li&gt;列表项 5&lt;/li&gt;
&lt;/ul&gt;
    </code></pre>
    <p>我们可以将此方法应用于列表项集合：</p>
    <pre><code>
$( "li" ).odd().css( "background-color", "red" );
    </code></pre>
    <p>此调用的结果是第二和第四项的背景为红色。</p>

=== nth-of-type-selector ===

<p>由于 jQuery 对 <code>:nth-</code> 选择器的实现严格源自 CSS 规范，<code>n</code> 的值是 "1-indexed"，这意味着计数从 1 开始。对于其他选择器表达式，如 <a href="/first/"><code>.first()</code></a> 或 <a href="/eq/"><code>.eq()</code></a>，jQuery 遵循 JavaScript 的 "0-indexed" 计数。</p>
    <p>有关此用法的进一步讨论可以在 <a href="https://www.w3.org/TR/css3-selectors/#nth-of-type-pseudo">W3C CSS 规范</a> 中找到。</p>

=== nth-last-of-type-selector ===

<p>由于 jQuery 对 <code>:nth-</code> 选择器的实现严格源自 CSS 规范，<code>n</code> 的值是 "1-indexed"，这意味着计数从 1 开始。对于其他选择器表达式，如 <a href="/first/"><code>.first()</code></a> 或 <a href="/eq/"><code>.eq()</code></a>，jQuery 遵循 JavaScript 的 "0-indexed" 计数。给定一个包含三个 <code>&lt;li&gt;</code> 的单个 <code>&lt;ul&gt;</code>，<code>$('li:nth-last-of-type(1)')</code> 选择第三个、最后一个 <code>&lt;li&gt;</code>。</p>
    <p>有关此用法的进一步讨论可以在 <a href="https://www.w3.org/TR/css3-selectors/#nth-last-of-type-pseudo">W3C CSS 规范</a> 中找到。</p>

=== nth-last-child-selector ===

<p>由于 jQuery 对 <code>:nth-</code> 选择器的实现严格源自 CSS 规范，<code>n</code> 的值是 "1-indexed"，这意味着计数从 1 开始。对于其他选择器表达式，如 <a href="/first/"><code>.first()</code></a> 或 <a href="/eq/"><code>.eq()</code></a>，jQuery 遵循 JavaScript 的 "0-indexed" 计数。给定一个包含三个 <code>&lt;li&gt;</code> 的单个 <code>&lt;ul&gt;</code>，<code>$( "li:nth-last-child(1)" )</code> 选择第三个、最后一个 <code>&lt;li&gt;</code>。</p>
    <p>有关此用法的进一步讨论可以在 <a href="https://www.w3.org/TR/css3-selectors/#nth-last-child-pseudo">W3C CSS 规范</a> 中找到。</p>

=== not ===

<p>给定一个表示一组 DOM 元素的 jQuery 对象，<code>.not()</code> 方法从匹配元素的子集中构造一个新的 jQuery 对象。提供的选择器会针对每个元素进行测试；不匹配选择器的元素将包含在结果中。</p>
    <p>考虑一个页面上有一个简单列表：</p>
    <pre><code>
&lt;ul&gt;
  &lt;li&gt;列表项 1&lt;/li&gt;
  &lt;li&gt;列表项 2&lt;/li&gt;
  &lt;li&gt;列表项 3&lt;/li&gt;
  &lt;li&gt;列表项 4&lt;/li&gt;
  &lt;li&gt;列表项 5&lt;/li&gt;
&lt;/ul&gt;
    </code></pre>
    <p>我们可以将此方法应用于列表项集合：</p>
    <pre><code>
$( "li" ).not( ":nth-child(2n)" ).css( "background-color", "red" );
    </code></pre>
    <p>此调用的结果是列表项 1、3 和 5 的背景为红色，因为它们不匹配选择器。</p>
    <h4>移除特定元素</h4>
    <p><code>.not()</code> 方法的第二个版本允许我们从匹配集移除元素，前提是我们之前通过其他方式找到了这些元素。例如，假设我们的列表中有一个项应用了 id：</p>
    <pre><code>
&lt;ul&gt;
  &lt;li&gt;列表项 1&lt;/li&gt;
  &lt;li&gt;列表项 2&lt;/li&gt;
  &lt;li id="notli"&gt;列表项 3&lt;/li&gt;
  &lt;li&gt;列表项 4&lt;/li&gt;
  &lt;li&gt;列表项 5&lt;/li&gt;
&lt;/ul&gt;
    </code></pre>
    <p>我们可以使用原生 JavaScript 的 <code>getElementById()</code> 函数获取第三个列表项，然后将其从 jQuery 对象中移除：</p>
    <pre><code>
$( "li" ).not( document.getElementById( "notli" ) )
  .css( "background-color", "red" );
    </code></pre>
    <p>此语句将列表项 1、2、4 和 5 的颜色更改为红色。我们可以通过更简单的 jQuery 表达式完成相同的操作，但当其他库提供对普通 DOM 节点的引用时，这种技术可能会很有用。</p>
    <p>从 jQuery 1.4 开始，<code>.not()</code> 方法可以像 <code>.filter()</code> 一样接受一个函数作为其参数。对于函数返回 <code>true</code> 的元素，将从过滤后的集合中排除；所有其他元素将被包含。</p>
    <p><strong>注意：</strong>当将 CSS 选择器字符串传递给 <code>.not()</code> 时，文本和注释节点将在过滤过程中始终从结果 jQuery 对象中移除。当提供特定节点或节点数组时，只有在它们与过滤数组中的节点匹配时，文本或注释节点才会从 jQuery 对象中移除。</p>

=== not-selector ===

<p>所有选择器都可以在<code>:not()</code>中使用，例如：<code>:not(div a)</code>和<code>:not(div,a)</code>。</p>
    <h3>附加说明</h3>
    <p>使用<code><a href="/not/">.not()</a></code>方法可以提供更易读的选择，而不是将复杂的选择器或变量推入<code>:not()</code>选择器过滤器。在大多数情况下，这是更好的选择。</p>

=== nextUntil ===

<p>给定一个表示一组DOM元素的选择器表达式，<code>.nextUntil()</code>方法在DOM树中搜索这些元素的后续元素，直到达到由方法参数匹配的元素为止。返回的新jQuery对象包含所有后续兄弟元素，直到但不包括由<code>.nextUntil()</code>参数匹配的元素。</p>
    <p>如果选择器没有匹配或未提供，将选择所有后续兄弟元素；在这些情况下，它选择的元素与<code>.nextAll()</code>方法在未提供过滤选择器时选择的元素相同。</p>
    <p><strong>自jQuery 1.6起</strong>，可以将DOM节点或jQuery对象传递给<code>.nextUntil()</code>方法，而不是选择器。</p>
    <p>该方法可选地接受一个选择器表达式作为第二个参数。如果提供了该参数，将通过测试元素是否匹配来过滤这些元素。</p>

=== next ===

<p>给定一个表示一组DOM元素的jQuery对象，<code>.next()</code>方法允许我们在DOM树中搜索这些元素的紧接着的兄弟元素，并从匹配的元素构建一个新的jQuery对象。</p>
    <p>该方法可选地接受一个与我们可以传递给<code>$()</code>函数相同类型的选择器表达式。如果紧接着的兄弟元素匹配选择器，则它将保留在新构建的jQuery对象中；否则，它将被排除。</p>
    <p>考虑一个页面上有一个简单的列表：</p>
    <pre><code>
&lt;ul&gt;
  &lt;li&gt;列表项 1&lt;/li&gt;
  &lt;li&gt;列表项 2&lt;/li&gt;
  &lt;li class="third-item"&gt;列表项 3&lt;/li&gt;
  &lt;li&gt;列表项 4&lt;/li&gt;
  &lt;li&gt;列表项 5&lt;/li&gt;
&lt;/ul&gt;
    </code></pre>
    <p>如果我们从第三项开始，可以找到紧接着它的元素：</p>
    <pre><code>
$( "li.third-item" ).next().css( "background-color", "red" );
    </code></pre>
    <p>此调用的结果是项4的背景变为红色。由于我们没有提供选择器表达式，因此这个后续元素毫无疑问地包含在对象中。如果我们提供了一个，元素将在包含之前进行匹配测试。</p>

=== next-siblings-selector ===

<p>(<code>prev + next</code>)和(<code>prev ~ siblings</code>)之间的显著区别在于它们各自的范围。前者仅达到紧接着的兄弟元素，而后者将范围扩展到所有后续兄弟元素。</p>

=== next-adjacent-selector ===

<p>在考虑下一个相邻兄弟选择器(<code>prev + next</code>)和一般兄弟选择器(<code>prev ~ siblings</code>)时，一个重要的点是组合符两侧的元素必须共享相同的父元素。</p>

=== multiple-selector ===

<p>您可以指定任意数量的选择器以组合成单个结果。这个多重表达式组合器是一种高效选择不同元素的方法。返回的 jQuery 对象中的 DOM 元素顺序可能与文档顺序不完全相同。此组合器的替代方法是 .<a href="/add/">add()</a> 方法。</p>

=== multiple-attribute-selector ===


=== map ===

<p>如果您希望处理一个普通数组或对象，请使用 <a href="/jQuery.map/">jQuery.map()</a>。</p>
    <p>由于返回值是一个包含数组的 jQuery 对象，因此通常会在结果上调用 <code>.get()</code> 来处理基本数组。</p>
    <p><code>.map()</code> 方法特别适用于获取或设置一组元素的值。考虑一个包含一组复选框的表单：</p>
    <pre><code>
&lt;form method="post" action=""&gt;
  &lt;fieldset&gt;
    &lt;div&gt;
      &lt;label for="two"&gt;2&lt;/label&gt;
      &lt;input type="checkbox" value="2" id="two" name="number[]"&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;label for="four"&gt;4&lt;/label&gt;
      &lt;input type="checkbox" value="4" id="four" name="number[]"&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;label for="six"&gt;6&lt;/label&gt;
      &lt;input type="checkbox" value="6" id="six" name="number[]"&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;label for="eight"&gt;8&lt;/label&gt;
      &lt;input type="checkbox" value="8" id="eight" name="number[]"&gt;
    &lt;/div&gt;
  &lt;/fieldset&gt;
&lt;/form&gt;
    </code></pre>
    <p>要获取复选框 <code>ID</code> 的逗号分隔列表：</p>
    <pre><code>
$( ":checkbox" )
  .map(function() {
    return this.id;
  })
  .get()
  .join();
    </code></pre>
    <p>此调用的结果是字符串 <code>"two,four,six,eight"</code>。</p>
    <p>在回调函数中，<code>this</code> 指的是每次迭代的当前 DOM 元素。该函数可以返回单个数据项或要插入结果集的数据项数组。如果返回一个数组，则数组中的元素将被插入到集合中。如果函数返回 <code>null</code> 或 <code>undefined</code>，则不会插入任何元素。</p>

=== lt-selector ===

<div class="warning">
      <p><strong>从 jQuery 3.4 开始</strong>，<code>:lt</code> 伪类已被弃用。请从您的选择器中移除它，并在后续使用 <a href="/slice/"><code>.slice()</code></a> 过滤结果。例如，<code>:lt(3)</code> 可以用 <code>.slice( 0, 3 )</code> 替代。</p>
    </div>
    <p>
      <strong>与索引相关的选择器</strong>
    </p>
    <p>与索引相关的选择器（包括这个“少于”选择器）过滤已匹配的元素集合。它们根据该匹配集中元素的顺序缩小集合。例如，如果首先使用类选择器（<code>.myclass</code>）选择元素并返回四个元素，则这些元素在这些选择器中被赋予索引 0 到 3。</p>
    <p>请注意，由于 JavaScript 数组使用 <em>0 基索引</em>，这些选择器反映了这一事实。这就是为什么 <code>$( ".myclass:lt(1)" )</code> 选择文档中具有类 <code>myclass</code> 的第一个元素，而不是选择没有元素。相比之下，<code>:nth-child(n)</code> 使用 <em>1 基索引</em> 以符合 CSS 规范。</p>
    <p>在 jQuery 1.8 之前，<code>:lt(index)</code> 选择器不接受 <code>index</code> 的负值。</p>

=== live ===

<div class="warning">
      <p>注意：此 API 在 jQuery 1.9 中已被移除；请改用 <a href="/on/"><code>on()</code></a>。</p>
    </div>
    <p>此方法提供了一种将委托事件处理程序附加到页面的 <code>document</code> 元素的方式，这简化了在动态添加内容到页面时使用事件处理程序的过程。有关直接事件与委托事件的讨论，请参见 <a href="/on/"><code>.on()</code></a> 方法以获取更多信息。</p>
    <p>将 <code>.live()</code> 方法重写为其后继方法是简单的；以下是所有三种事件附加方法的等效调用模板：</p>
    <pre><code>
$( selector ).live( events, data, handler );                // jQuery 1.3+
$( document ).delegate( selector, events, data, handler );  // jQuery 1.4.3+
$( document ).on( events, selector, data, handler );        // jQuery 1.7+
    </code></pre>
    <p><code>events</code> 参数可以是事件类型名称和可选命名空间的空格分隔列表，或者是事件名称字符串和处理程序的对象。<code>data</code> 参数是可选的，可以省略。例如，以下三个方法调用在功能上是等效的（但请参见下面更有效和高效的附加委托事件处理程序的方法）：</p>
    <pre><code>
$( "a.offsite" ).live( "click", function() {
  alert( "再见！" ); // jQuery 1.3+
});
$( document ).delegate( "a.offsite", "click", function() {
  alert( "再见！" ); // jQuery 1.4.3+
});
$( document ).on( "click", "a.offsite", function() {
  alert( "再见！" );  // jQuery 1.7+
});
    </code></pre>
    <p>由于后续版本的 jQuery 提供了更好的方法，<code>.live()</code> 方法不再推荐使用，这些方法没有它的缺点。特别是，使用 <code>.live()</code> 会出现以下问题：</p>
    <ul>
      <li>jQuery 尝试在调用 <code>.live()</code> 方法之前检索选择器指定的元素，这在大型文档中可能耗时。</li>
      <li>不支持方法链。例如，<code>$( "a" ).find( ".offsite, .external" ).live( ... ); </code> 是 <em>无效的</em>，并且无法按预期工作。</li>
      <li>由于所有 <code>.live()</code> 事件都附加在 <code>document</code> 元素上，事件在处理之前会走最慢的路径。</li>
      <li>在移动 iOS（iPhone、iPad 和 iPod Touch）上，<code>click</code> 事件不会冒泡到大多数元素的文档主体，并且不能与 <code>.live()</code> 一起使用，而不应用以下解决方法之一：
        <ol>
          <li>使用本地可点击的元素，如 <code>a</code> 或 <code>button</code>，因为这两者都会冒泡到 <code>document</code>。</li>
          <li>使用 <code>.on()</code> 或 <code>.delegate()</code> 附加到低于 <code>document.body</code> 的元素，因为移动 iOS 会在主体内冒泡。</li>
          <li>对需要冒泡点击的元素（或包含 <code>document.documentElement</code> 的父元素）应用 CSS 样式 <code>cursor:pointer</code>。但请注意，这将禁用该元素的复制/粘贴，并在触摸时使其高亮。</li>
        </ol>
      </li>
      <li>在事件处理程序中调用 <a href="/event.stopPropagation/"><code>event.stopPropagation()</code></a> 无法有效阻止附加在文档中较低位置的事件处理程序；事件已经传播到 <code>document</code>。</li>
      <li><code>.live()</code> 方法与其他事件方法的交互方式可能会令人惊讶，例如，<code>$( document ).off( "click" )</code> 会移除通过任何调用 <code>.live()</code> 附加的所有点击处理程序！</li>
    </ul>
    <p>对于仍在使用 <code>.live()</code> 的页面，此版本特定差异列表可能会有所帮助：</p>
    <ul>
      <li>在 jQuery 1.7 之前，要在使用 <code>.live()</code> 绑定后停止进一步的处理程序执行，处理程序必须返回 <code>false</code>。调用 <code>.stopPropagation()</code> 将无法实现此目的。</li>
      <li>从 <b>jQuery 1.4</b> 开始，<code>.live()</code> 方法支持自定义事件以及 <em>所有冒泡的 JavaScript 事件</em>。它还支持某些不冒泡的事件，包括 <code>change</code>、<code>submit</code>、<code>focus</code> 和 <code>blur</code>。</li>
      <li>在 <b>jQuery 1.3.x</b> 中，仅能绑定以下 JavaScript 事件：<code>click</code>、<code>dblclick</code>、<code>keydown</code>、<code>keypress</code>、<code>keyup</code>、<code>mousedown</code>、<code>mousemove</code>、<code>mouseout</code>、<code>mouseover</code> 和 <code>mouseup</code>。</li>
    </ul>

=== last ===

<p>给定一个表示一组 DOM 元素的 jQuery 对象，<code>.last()</code> 方法从该集合的最后一个元素构造一个新的 jQuery 对象。</p>
    <p>考虑一个包含简单列表的页面：</p>
    <pre><code>
&lt;ul&gt;
  &lt;li&gt;列表项 1&lt;/li&gt;
  &lt;li&gt;列表项 2&lt;/li&gt;
  &lt;li&gt;列表项 3&lt;/li&gt;
  &lt;li&gt;列表项 4&lt;/li&gt;
  &lt;li&gt;列表项 5&lt;/li&gt;
&lt;/ul&gt;
    </code></pre>
    <p>我们可以将此方法应用于列表项集合：</p>
    <pre><code>
$( "li" ).last().css( "background-color", "red" );
    </code></pre>
    <p>此调用的结果是最后一项的背景为红色。</p>

=== last-of-type-selector ===

<p><code>:last-of-type</code> 选择器匹配在文档树中没有其他同一父元素和同一元素名称的元素。</p>

=== keypress ===

<div class="warning">
      <p>本页面描述了 <code>keypress</code> 事件。有关已弃用的 <code>.keypress()</code> 方法，请参见 <a href="/keypress-shorthand/"><code>.keypress()</code></a>。</p>
    </div>
    <p><strong>注意：</strong>由于 <code>keypress</code> 事件未被任何官方规范覆盖，因此在使用时遇到的实际行为可能因浏览器、浏览器版本和平台而异。</p>
    <p>当浏览器注册键盘输入时，<code>keypress</code> 事件会发送到元素。这类似于 <code>keydown</code> 事件，除了修饰键和非打印键（如 <kbd>Shift</kbd>、<kbd>Esc</kbd> 和 <kbd>delete</kbd>）会触发 <code>keydown</code> 事件，但不会触发 <code>keypress</code> 事件。根据平台和浏览器，两个事件之间可能会出现其他差异。</p>
    <p><code>keypress</code> 事件处理程序可以附加到任何元素，但事件仅发送到获得焦点的元素。可聚焦的元素在不同浏览器之间可能有所不同，但表单控件始终可以获得焦点，因此是此事件类型的合理候选者。</p>
    <p>例如，考虑以下 HTML：</p>
    <pre><code>
&lt;form&gt;
  &lt;fieldset&gt;
    &lt;input id="target" type="text" value="Hello there"&gt;
  &lt;/fieldset&gt;
&lt;/form&gt;
&lt;div id="other"&gt;
  触发处理程序
&lt;/div&gt;
    </code></pre>
    <p>事件处理程序可以绑定到输入字段：</p>
    <pre><code>
$( "#target" ).on( "keypress", function() {
  console.log( "处理程序 `keypress` 被调用。" );
} );
    </code></pre>
    <p>现在，当插入点在字段内时，按下一个键会显示日志：</p>
    <p>
      <samp>处理程序 `keypress` 被调用。</samp>
    </p>
    <p>要手动触发事件，请使用 <code>.trigger( "keypress" )</code>：</p>
    <pre><code>
$( "#other" ).on( "click", function() {
  $( "#target" ).trigger( "keypress" );
} );
    </code></pre>
    <p>在此代码执行后，点击 <samp>触发处理程序</samp> div 也会记录该消息。</p>
    <p>如果需要捕获任何地方的按键（例如，在页面上实现全局快捷键），将此行为附加到 <code>document</code> 对象是有用的。由于事件冒泡，所有按键都会向上冒泡到 <code>document</code> 对象，除非明确停止。</p>
    <p>要确定输入了哪个字符，请检查传递给处理程序函数的 <code>event</code> 对象。虽然浏览器使用不同的属性来存储此信息，但 jQuery 规范化了 <code>.which</code> 属性，因此您可以可靠地使用它来检索字符代码。</p>
    <p>请注意，<code>keydown</code> 和 <code>keyup</code> 提供一个代码，指示哪个键被按下，而 <code>keypress</code> 指示输入了哪个字符。例如，小写字母 "a" 将被 <code>keydown</code> 和 <code>keyup</code> 报告为 65，但被 <code>keypress</code> 报告为 97。大写字母 "A" 在所有事件中都报告为 65。由于这种区别，在捕获特殊按键（如箭头键）时，<code>.keydown()</code> 或 <code>.keyup()</code> 是更好的选择。</p>

=== jQuery ===

<h4 id="creating-new-elements">创建新元素</h4>
      <p>如果将字符串作为参数传递给 <code>$()</code>，jQuery 会检查该字符串是否看起来像 HTML（即，它以 <code>&lt;tag ... &gt;</code> 开头）。如果不是，则将字符串解释为选择器表达式，如上所述。但如果字符串似乎是一个 HTML 片段，jQuery 会尝试根据 HTML 创建新的 DOM 元素。然后创建并返回一个 jQuery 对象，引用这些元素。您可以对该对象执行任何常规的 jQuery 方法：</p>
      <pre><code>$( "&lt;p id='test'&gt;我的 &lt;em&gt;新&lt;/em&gt; 文本&lt;/p&gt;" ).appendTo( "body" );</code></pre>
      <p>要显式解析字符串为 HTML，请使用 <a href="/jQuery.parseHTML/">$.parseHTML()</a> 方法。</p>
      <p>默认情况下，元素是使用与加载 jQuery 库的文档匹配的 <code>.ownerDocument</code> 创建的。要将元素注入到不同的文档中，应使用该文档创建，例如，<code>$("&lt;p&gt;hello iframe&lt;/p&gt;", $("#myiframe").prop("contentWindow").document)</code>。</p>
      <p>如果 HTML 比没有属性的单个标签更复杂，如上面的示例，元素的实际创建由浏览器的 <code>.innerHTML</code> 机制处理。在大多数情况下，jQuery 创建一个新的 <code>&lt;div&gt;</code> 元素，并将该元素的 <code>innerHTML</code> 属性设置为传入的 HTML 片段。当参数具有单个标签（带可选的闭合标签或快速闭合）时 — <code>$( "&lt;img&#xA0;/&gt;" )</code> 或 <code>$( "&lt;img&gt;" )</code>，<code>$( "&lt;a&gt;&lt;/a&gt;" )</code> 或 <code>$( "&lt;a&gt;" )</code> — jQuery 使用原生 JavaScript <code>.createElement()</code> 函数创建元素。</p>
      <p>在传入复杂 HTML 时，一些浏览器可能不会生成完全复制提供的 HTML 源的 DOM。如前所述，jQuery 使用浏览器的 <code>.innerHTML</code> 属性解析传递的 HTML 并将其插入到当前文档中。在此过程中，一些浏览器会过滤掉某些元素，例如 <code>&lt;html&gt;</code>、<code>&lt;title&gt;</code> 或 <code>&lt;head&gt;</code> 元素。因此，插入的元素可能不代表传递的原始字符串。</p>
      <p>过滤并不限于这些标签。例如，Internet Explorer 8 之前的版本还会将所有链接上的 <code>href</code> 属性转换为绝对 URL，而 Internet Explorer 9 之前的版本在没有添加单独的 <a href="https://github.com/afarkas/html5shiv">兼容层</a> 的情况下将无法正确处理 HTML5 元素。</p>
      <p>为了确保跨平台兼容性，片段必须是良构的。可以包含其他元素的标签应配对闭合标签：</p>
      <pre><code>$( "&lt;a href='https://jquery.com'&gt;&lt;/a&gt;" );</code></pre>
      <p>不能包含元素的标签可以快速闭合或不闭合：</p>
      <pre><code>
$( "&lt;img /&gt;" );
$( "&lt;input&gt;" );
      </code></pre>
      <p>在将 HTML 传递给 <code>jQuery()</code> 时，请注意文本节点不被视为 DOM 元素。除了少数方法（如 <code>.content()</code>）外，它们通常会被忽略或移除。例如：</p>
      <pre><code>
var el = $( "&lt;br&gt;2&lt;br&gt;3" ); // 返回 [&lt;br&gt;, "2", &lt;br&gt;]
el = $( "&lt;br&gt;2&lt;br&gt;3 &gt;" ); // 返回 [&lt;br&gt;, "2", &lt;br&gt;, "3 &amp;gt;"]
      </code></pre>
      <p>这种行为是预期的。<strong>从 jQuery 1.9.0 开始</strong>（除非使用 <a href="https://github.com/jquery/jquery-migrate/#readme">jQuery Migrate 插件</a>），<code>jQuery()</code> 要求 HTML 字符串以 <code>&lt;</code> 开头（即文本节点不能出现在 HTML 字符串的前面）。</p>
      <p><strong>从 jQuery 1.4 开始</strong>，<code>jQuery()</code> 的第二个参数可以接受一个普通对象，该对象由可以传递给 <a href="/attr/">.attr()</a> 方法的属性的超集组成。</p>
      <p><strong>重要：</strong>如果传递第二个参数，则第一个参数中的 HTML 字符串必须表示一个没有属性的简单元素。<strong>从 jQuery 1.4 开始</strong>，可以传递任何 <a href="/category/events/">事件类型</a>，并且可以调用以下 jQuery 方法：<a href="/val/">val</a>、<a href="/css/">css</a>、<a href="/html/">html</a>、<a href="/text/">text</a>、<a href="/data/">data</a>、<a href="/width/">width</a>、<a href="/height/">height</a> 或 <a href="/offset/">offset</a>。</p>
      <p><strong>从 jQuery 1.8 开始</strong>，可以将任何 jQuery 实例方法（<code>jQuery.fn</code> 的方法）用作传递给第二个参数的对象的属性：</p>
      <pre><code>
$( "<div></div>", {
  "class": "my-div",
  on: {
    touchstart: function( event ) {
      // 做一些事情
    }
  }
}).appendTo( "body" );
      </code></pre>
      <p>由于 <code>"class"</code> 是 JavaScript 保留字，因此在对象中必须用引号引起来，而 <code>"className"</code> 不能使用，因为它指的是 DOM 属性，而不是属性。</p>
      <p>虽然第二个参数很方便，但其灵活性可能导致意外后果（例如，<code>$( "&lt;input&gt;", {size: "4"} )</code> 调用 <code>.size()</code> 方法，而不是设置 size 属性）。因此，前面的代码块可以改写为：</p>
<pre><code>
$( "<div></div>" )
  .addClass( "my-div" )
  .on({
    touchstart: function( event ) {
      // 做一些事情
    }
  })
    .appendTo( "body" );
      </code></pre>

